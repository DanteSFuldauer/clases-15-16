<h1>Lista de productos</h1>

<ul> 
    {{!Metodo foreach en handlbars}}
    {{#each productos}}
        {{#if status}} {{!si el atributo status es verdadero}}
        {{!consulto por atributos del objeto}}
            <h3>Producto {{nombre}}</h3>
            <p>Marca: {{marca}}</p>
            <p>Precio: ${{precio}}</p>
            <p>Stock: {{stock}} unidades</p>
        {{/if}}
    {{/each}}
</ul>